const firstName = 'Pero';
const lastName = 'Peric';
const fullName = firstName + (" ") + lastName;

console.log('Prvi zadatak: ' + fullName);

let y = 10;
let z = 2;


if( z === 2 ) {
    console.log(x = y);
}

else {
    console.log(x = 5);
}

for(let i = 1; i <= 20; i++) {
    if(i % 2 == 0) {
        console.log('Broj ' + i + ' je paran.');
    }
    else {
        console.log('Broj ' + i + ' je neparan.');
    }
}
